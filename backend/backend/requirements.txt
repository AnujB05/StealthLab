fastapi>=0.115
uvicorn[standard]>=0.32
asyncpg>=0.30
pydantic>=2.9
pydantic-settings>=2.6
python-dotenv>=1.0

# Panel agents (Section 7). openai covers both OpenAI and Fireworks/Kimi K3,
# which is OpenAI-compatible and differs only in base_url.
anthropic>=0.40
openai>=1.55

# Embeddings (not yet wired -- retrieval currently uses graph traversal only)
voyageai>=0.3

# Optional: YAML workflow specs for onboarding
PyYAML>=6.0

# Dev
pytest>=8.0
pytest-asyncio>=0.24
